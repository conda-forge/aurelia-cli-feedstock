{% set version = "3.0.8" %}

package:
  name: aurelia-cli
  version: {{ version }}

source:
  url: https://registry.npmjs.org/aurelia-cli/-/aurelia-cli-{{ version }}.tgz
  sha256: 4d0dcac47eb9aee39154a6df9e3eba9e08bc5828eb33575afcf32c173a6abf0c

build:
  number: 0
  skip: true  # [win or ppc64le]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ stdlib('c') }}
    - make
    - nodejs
    - pnpm
    - pnpm-licenses
  host:
    - nodejs
  run:
    - nodejs

test:
  commands:
    - aurelia --help

about:
  license: MIT
  license_file:
    - LICENSE
    - third-party-licenses.txt
  summary: The command line tooling for Aurelia.
  home: http://aurelia.io/
  dev_url: https://github.com/aurelia/cli

extra:
  recipe-maintainers:
    - danielnachun
